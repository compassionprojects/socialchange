<nav class="navbar navbar-expand-lg">
  <div class="container py-2">
    <a class="navbar-brand" href="/"><%= t('application_name') %></a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#menu" aria-controls="menu" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse justify-content-end" id="menu">
      <ul class="navbar-nav mr-auto mb-2 mb-lg-0">
        <% if user_signed_in? %>
          <li class="nav-item">
            <%= link_to t('users.invite'), new_user_invitation_path, :class => "nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to t('users.profile'), edit_user_registration_path, :class => "nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to t('users.logout'), destroy_user_session_path, data: { turbo_method: :delete }, :class => "nav-link" %>
          </li>
        <% else %>
          <li class="nav-item">
              <%= link_to t('users.signup'), new_registration_path(:user), :class => "nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to t('users.login'), new_user_session_path, :class => "nav-link" %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
